node_modules/
__pycache__/
*.pyc
data/
.DS_Store

# Secrets must never reach this repo - it is public.
# .dockerignore only keeps files out of an image; it does NOT stop git committing them.
.env
.env.*
*.pem
*.key
*_SECRET*
*secret*.json
*credentials*.json
azure.json
